# New Tab Dammit

A tiny WordPress must-use plugin.

The WordPress admin-bar link for visiting the site should open the site in a new tab, dammit.

Drop `new-tab-dammit.php` into `wp-content/mu-plugins/`.
